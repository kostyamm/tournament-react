import { Button, Empty } from 'antd';
import { Fragment } from 'react';

export const Home = () => {
    return (
        <Fragment>
            <Empty />
            <Button type={'primary'}>Button</Button>
        </Fragment>
    );
};
